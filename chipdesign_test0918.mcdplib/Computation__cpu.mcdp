dp{
    # global req & pro
    requires Computation__cpu__reqCost [dimensionless]
    requires Computation__cpu__reqPower [dimensionless]
    provides Computation__cpu__proThroughtput [dimensionless]

    # database
    implemented-by yaml resource("Computation__cpu.yaml")
}
#mcdp-solve -d test_computation.mcdplib Computation__cpu '9' --imp --plot --out 'test_computation.mcdplib.outputs'