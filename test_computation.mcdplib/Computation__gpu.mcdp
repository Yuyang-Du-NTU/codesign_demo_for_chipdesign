dp{
    # global req & pro
    requires Computation__gpu__reqCost [dimensionless]
    requires Computation__gpu__reqPower [dimensionless]
    provides Computation__gpu__proThroughtput [dimensionless]

    # database
    implemented-by yaml resource("Computation__gpu.yaml")
}

#mcdp-solve -d test_computation.mcdplib Computation__gpu '9' --imp --plot --out 'test_computation.mcdplib.outputs'